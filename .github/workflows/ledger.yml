name: Ironbound Ledger

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run-ledger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: python -m pip install --upgrade pip requests

      - name: Test Discord webhook
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -s -H "Content-Type: application/json" \
            -d '{"content":"✅ Ironbound Ledger webhook test (from GitHub Actions)"}' \
            "$DISCORD_WEBHOOK_URL"

      - name: Run ledger
        env:
          SLEEPER_LEAGUE_ID: ${{ secrets.SLEEPER_LEAGUE_ID }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python ledger.py

      - name: Commit updated state.json (if changed)
        run: |
          if git status --porcelain | grep -q "state.json"; then
            git config user.name "ironbound-ledger-bot"
            git config user.email "actions@users.noreply.github.com"
            git add state.json
            git commit -m "Update ledger state"
            git push
          else
            echo "No state changes."
          fi
